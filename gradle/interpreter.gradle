

task setupInterpreter { 
    description = "Prepare the chosen interpreter to run benchmarks"

    outputs.upToDateWhen { false }
}

if(project.hasProperty("gnu")) {
    apply from: './gradle/gnu.gradle'

} else {
    apply from: './gradle/renjin.gradle'
}


